# 函数解读
`forentend/src/utils/common.ts`

- 两个函数：`listToTree`和`formatTree`
- `listToTree`函数将一个扁平的列表转换成一个树形结构。列表中的每个元素都有id和parent_id属性。
- `formatTree`函数将树形结构（或列表）转换成一种特定的格式，每个节点包含`value`、`label`和`disabled`属性，以及可能的`children`。
- 使用这两个函数将部门列表转换为树形结构，并格式化为前端需要的选项格式。

# 步骤解读：

## listToTree函数：

- 创建一个映射表，键是节点的id，值是节点的浅拷贝（保留原始字段）。

- 遍历列表，对于每个节点，如果它有父节点（parent_id存在且在映射表中），则将自己放入父节点的children数组中。

- 如果没有父节点（parent_id为null或undefined），则将其作为根节点放入tree数组中。

注意：这里使用了同一个映射表来存储节点，并且每个节点在映射表中只有一份，所以修改映射表中的节点（比如添加children）会影响所有引用。

## formatTree函数：

- 接收一个节点数组（可以是树形结构的根节点数组），对每个节点进行格式化。

- 每个节点被格式化为一个对象，包含value（id）、label（name）和disabled（根据status字段判断）属性。

- 如果节点有children，则递归格式化children，并将结果赋值给当前节点的children属性。

## 使用示例(部门)：

- 首先调用`DeptAPI.listDept()`获取部门列表（扁平结构）。

- 然后使用`listToTree`将扁平列表转换为树形结构。

- 最后使用`formatTree`将树形结构格式化为`前端需要的树形选项结构`。

注意：在formatTree中，使用了Object.assign来添加children属性，如果原节点有children，则会被覆盖。

但是，注意看listToTree函数，它返回的树形结构的每个节点都是原始节点的浅拷贝，并且已经添加了children属性（如果有子节点）。
然后formatTree函数会遍历这个树形结构，并重新构建一个具有新结构的对象（value、label、disabled、children）。

因此，最终deptTreeData.value是一个树形结构，每个节点具有：
value: 部门id
label: 部门名称
disabled: 根据部门状态（status）判断，如果status为false或字符串"false"则为true（禁用），否则为false（不禁用）。
children: 子节点数组，同样结构的节点。

这样的结构适合用于树形选择组件。

- **这段代码是用于将扁平结构的部门列表转换为树形结构，并进行格式化处理，通常用于前端树形选择组件。**

## 主要功能模块

### 1. `listToTree` 函数 - 扁平转树形
```typescript
export function listToTree(list: any[]) {
  const map: { [key: string | number]: any } = {};
  // 创建映射表，保留每个节点的 parent_id 等原始字段
  list.forEach((item) => {
    map[item.id] = { ...item };
  });

  const tree: any[] = [];
  list.forEach((item) => {
    const parentId = item.parent_id;
    if (parentId && map[parentId]) {
      // 将当前节点加入其父节点的 children 数组中
      if (!map[parentId].children) {
        map[parentId].children = [];
      }
      map[parentId].children.push(map[item.id]);
    } else if (parentId === null || parentId === undefined) {
      // 根节点
      tree.push(map[item.id]);
    }
  });

  return tree;
}
```

**功能**：将扁平数组转换为树形结构
- **输入**：`[{id: 1, parent_id: null, name: 'A'}, {id: 2, parent_id: 1, name: 'B'}]`
- **输出**：`[{id: 1, parent_id: null, name: 'A', children: [{id: 2, parent_id: 1, name: 'B'}]}]`

**算法逻辑**：
1. **创建映射表**：将每个节点按 `id` 存入 `map` 对象，便于快速查找
2. **构建父子关系**：遍历每个节点，根据 `parent_id` 找到父节点，将当前节点添加到父节点的 `children` 数组中
3. **识别根节点**：`parent_id` 为 `null` 或 `undefined` 的节点作为树的根节点

### 2. `formatTree` 函数 - 格式化树形数据
```typescript
export function formatTree(nodes: any[]): any[] {
  return nodes.map((node) => {
    const formattedNode = {
      value: node.id,        // 选项值（部门ID）
      label: node.name,      // 显示标签（部门名称）
      disabled: node.status === false || String(node.status) === "false", // 是否禁用
    };

    if (node.children && node.children.length > 0) {
      Object.assign(formattedNode, { children: formatTree(node.children) });
    }

    return formattedNode;
  });
}
```

**功能**：将树形数据格式化为前端UI组件所需的格式
- **转换规则**：
  - `id` → `value`
  - `name` → `label`
  - 根据 `status` 字段设置 `disabled`
  - 递归处理子节点

**状态处理逻辑**：
- `status === false` 或 `String(status) === "false"` 时，选项被禁用
- 适用于部门停用、禁用等场景

### 3. 使用示例
```typescript
const deptResponse = await DeptAPI.listDept();
deptTreeData.value = formatTree(listToTree(deptResponse.data.data));
```

**数据处理流程**：
```
API返回的扁平数据 → listToTree() → 树形结构 → formatTree() → 前端可用的树形选项数据
```

## 示例数据转换

**原始数据**（扁平结构）：
```json
[
  {id: 1, parent_id: null, name: '总部', status: true},
  {id: 2, parent_id: 1, name: '技术部', status: true},
  {id: 3, parent_id: 1, name: '人事部', status: false}
]
```

**处理后**（树形结构）：
```json
[
  {
    value: 1,
    label: '总部',
    disabled: false,
    children: [
      {value: 2, label: '技术部', disabled: false},
      {value: 3, label: '人事部', disabled: true}
    ]
  }
]
```

## 应用场景
- **组织架构树**：显示部门层级关系
- **树形选择器**：如 Ant Design、Element UI 的 TreeSelect 组件
- **权限管理**：角色权限的树形分配
- **分类管理**：多级分类展示

## 代码特点
1. **类型灵活性**：使用 `any[]` 提高通用性，但牺牲了类型安全
2. **浅拷贝**：使用 `{ ...item }` 创建新对象，避免修改原始数据
3. **递归处理**：支持无限层级的树形结构
4. **状态管理**：根据部门状态控制选项是否可用

## 潜在改进点
```typescript
// 1. 添加类型定义
interface TreeNode {
  id: number;
  parent_id: number | null;
  name: string;
  status?: boolean;
  children?: TreeNode[];
}

interface FormattedNode {
  value: number;
  label: string;
  disabled: boolean;
  children?: FormattedNode[];
}

// 2. 性能优化：可以一次性遍历完成
export function listToTreeOptimized(list: TreeNode[]): TreeNode[] {
  const map = new Map<number, TreeNode>();
  const tree: TreeNode[] = [];
  
  list.forEach(item => {
    const node = { ...item };
    map.set(node.id, node);
  });
  
  list.forEach(item => {
    const node = map.get(item.id)!;
    if (item.parent_id && map.has(item.parent_id)) {
      const parent = map.get(item.parent_id)!;
      parent.children = parent.children || [];
      parent.children.push(node);
    } else {
      tree.push(node);
    }
  });
  
  return tree;
}
```

这种模式在前端处理组织架构、分类目录等树形数据时非常常见。